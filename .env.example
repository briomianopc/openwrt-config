# OpenWrt Config Generator - Environment Variables
# 复制此文件为 .env 并修改相应的值

# OpenWrt 源码目录路径（必须）
OPENWRT_SRC_PATH=/mnt/openwrt_source

# 静态文件目录（默认为当前目录）
STATIC_DIR=/app

# 生成的 menu.json 文件路径
OUTPUT_JSON_PATH=/app/menu.json

# 服务器监听端口
PORT=5000

# 调试模式（开发环境设为 True，生产环境设为 False）
DEBUG=False

# CORS 允许的源（生产环境建议设置具体域名，开发环境可使用 *）
# 多个域名用逗号分隔，例如: https://example.com,https://www.example.com
CORS_ORIGINS=*
